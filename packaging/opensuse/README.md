# openSUSE packaging scaffold

Tumbleweed packages will use RPM specs validated in an openSUSE buildroot and
published as a TunaOS rpm-md repository. This directory is intentionally a
scaffold until the target is enabled in the package-factory manifest.
