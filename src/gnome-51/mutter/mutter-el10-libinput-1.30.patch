diff -ur a/meson.build b/meson.build
--- a/meson.build	2026-08-27 20:27:23.491174423 +0000
+++ b/meson.build	2026-08-27 20:27:37.372764019 +0000
@@ -51,7 +51,7 @@
 wayland_protocols_req = '>= 1.48'
 
 # native backend version requirements
-libinput_req = '>= 1.31.0'
+libinput_req = '>= 1.30.0'
 gbm_req = '>= 21.3'
 libdrm_req = '>= 2.4.118'
 
diff -ur a/src/backends/native/meta-input-settings-native.c b/src/backends/native/meta-input-settings-native.c
--- a/src/backends/native/meta-input-settings-native.c	2026-08-27 20:27:23.495569091 +0000
+++ b/src/backends/native/meta-input-settings-native.c	2026-08-27 20:27:37.383625929 +0000
@@ -284,8 +284,11 @@
   if (!libinput_device)
     return;
 
-  if (libinput_device_config_dwt_is_available (libinput_device))
-    libinput_device_config_dwt_set_timeout (libinput_device, millis);
+  /* EL10 carries libinput 1.30, which predates
+   * libinput_device_config_dwt_set_timeout (added in 1.31); the timeout
+   * stays at libinput's default there. */
+  (void) libinput_device;
+  (void) millis;
 }
 
 static void
